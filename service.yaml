kind: Service
apiVersion: v1
metadata:
  name: aurora-gateway-service
  namespace: default
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    run: gateway
  type: ClusterIP
status:
  loadBalancer: {}